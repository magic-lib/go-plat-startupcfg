# 2026-06-15

## main/create_secret.go - 改为命令行参数输入
- 将 `originSecret` 和 `originKey` 的硬编码值改为通过 `flag` 包接收用户命令行输入
- `-s` 参数（必填）：原始明文字符串
- `-key` 参数（可选，默认空）：加密密钥
- 自定义 `flag.Usage` 提供详细的帮助信息（`-h` / `--help`）
- 新增错误提示信息，加密/解密失败时打印错误原因
